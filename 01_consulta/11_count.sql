-- Muestra la cantidad de email registrado en la tabla de users
SELECT COUNT(email) FROM hellosql.users;

-- Muestra la cantidad de age registrado en la tabla de users
SELECT COUNT(age) FROM hellosql.users;