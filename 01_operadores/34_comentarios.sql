/* Los comentarios se utilizan para explicar 
secciones de declaraciones SQL o para 
evitar la ejecución de declaraciones SQL. */

-- Los comentarios de una sola línea comienzan con --.

-- Cualquier texto entre -- y el final de la línea será ignorado (no se ejecutará).

SELECT * FROM Customers -- WHERE City='Berlin';

Comentarios de varias líneas
Los comentarios de varias líneas comienzan con /*y terminan con */.

Cualquier texto entre /* y */ será ignorado.